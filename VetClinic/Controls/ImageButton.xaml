<UserControl x:Class="VetClinic.Controls.ImageButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VetClinic.Controls"
             mc:Ignorable="d" 
             x:Name="Root"
             d:DesignHeight="450" d:DesignWidth="800">
    <Border x:Name="BorderElement">
        <Border.Style>
            <Style TargetType="{x:Type Border}">
                <Setter Property="Background" Value="{DynamicResource White}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="10,5"/>
                <Setter Property="BorderBrush" Value="{DynamicResource LightGray}"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
        </Border.Style>
        <StackPanel>
            <Image Source="{Binding Path=ImageSource, ElementName=Root}" Height="20" Width="20" VerticalAlignment="Center"/>
            <Button Content="{Binding Path=Content, ElementName=Root}" Command="{Binding Path=Command, ElementName=Root}"/>
        </StackPanel>       
    </Border>
</UserControl>
