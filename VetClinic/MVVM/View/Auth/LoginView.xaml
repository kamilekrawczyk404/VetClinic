<UserControl x:Class="VetClinic.MVVM.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VetClinic.MVVM.View"
             xmlns:controls="clr-namespace:VetClinic.Controls.Input"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Stretch" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="0,0,10,0">
            <TextBlock Style="{StaticResource Header}" Text="Welcome Back!"/>
            <TextBlock Style="{StaticResource Header}" Text="Your Pet's Best Health Starts Here." Margin="0,0,0,10"/>
            <Image Source="/Images/Hero/login.png" MaxWidth="400" MaxHeight="400"/>
        </StackPanel>
        
        <StackPanel Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="10,0,0,0" >
            <TextBlock Text="Create a new account"/>
            <local:Input />
            <local:Input />
            <local:Input />
            <local:Input />
        </StackPanel>
    </Grid>
</UserControl>
